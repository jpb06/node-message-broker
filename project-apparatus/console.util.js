var main = {};

main.printHeader = function (message) {
    let line = '--------------------------------------------------------------------------------';
    console.log(line + '\n' + message + '\n' + line);
};

module.exports = main;
